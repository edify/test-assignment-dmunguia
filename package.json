{
  "name": "edify-coding-test",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --testResultsProcessor='jest-html-reporter' --testEnvironment='node'"
  },
  "devDependencies": {
    "eslint": "^6.4.0",
    "jest": "^22.4.3",
    "jest-html-reporter": "^2.6.2",
    "plato": "^1.7.0"
  },
  "engines": {
    "node": ">=8"
  },
  "dependencies": {
    "moment": "^2.24.0"
  }
}
